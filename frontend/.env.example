# ══════════════════════════════════════════════════════════════
# FastCook — FRONTEND .env.example
#
# ⚠️  VARIÁVEIS PÚBLICAS APENAS (prefixo VITE_)
# Estas variáveis são INTENCIONALMENTE expostas ao browser.
# A segurança é garantida pelo Row Level Security (RLS) do Supabase.
#
# ✅  NUNCA coloque aqui: GROQ_API_KEY, SERVICE_ROLE_KEY
# ══════════════════════════════════════════════════════════════

# ── Supabase (público — restrito por RLS) ──────────────────────
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key-here
